"use client";import {FormEvent,useState} from "react";export default function Login(){const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMsg("Signing in…");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(r.ok){location.href="/dashboard";return}const d=await r.json();setMsg(d.error||"Unable to sign in")}return <main className="authWrap"><form className="card authCard" onSubmit={submit}><div className="eyebrow">FS SOFTWARES · SECURE ACCESS</div><h1>Welcome back.</h1><p>Sign in to manage cards, leads, analytics and organization controls.</p><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" required minLength={10}/></div><button className="btn btnPrimary" type="submit">Sign in</button>{msg&&<p className="success">{msg}</p>}<p>New to FS Digital Identity? <a href="/register"><strong>Create an account</strong></a></p></form></main>}
