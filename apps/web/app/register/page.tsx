"use client";import {FormEvent,useState} from "react";export default function Register(){const [msg,setMsg]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMsg("Creating account…");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/register",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({displayName:f.get("name"),email:f.get("email"),password:f.get("password")})});if(r.ok){location.href="/dashboard";return}const d=await r.json();setMsg(d.error||"Unable to register")}return <main className="authWrap"><form className="card authCard" onSubmit={submit}><div className="eyebrow">FS SOFTWARES · GET STARTED</div><h1>Create your identity.</h1><p>Start with your professional profile, then add organization governance when your team is ready.</p><div className="field"><label>Full name</label><input name="name" required/></div><div className="field"><label>Email</label><input name="email" type="email" required/></div><div className="field"><label>Password</label><input name="password" type="password" required minLength={10}/></div><button className="btn btnPrimary" type="submit">Create account</button>{msg&&<p className="success">{msg}</p>}<p>Already registered? <a href="/login"><strong>Sign in</strong></a></p></form></main>}
