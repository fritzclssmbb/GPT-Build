import { NextResponse } from 'next/server';import { currentUser } from '../../../../../lib/auth';import { query } from '../../../../../lib/db';
async function owns(id:string,uid:string){return (await query(`SELECT id FROM cards WHERE id=$1 AND owner_user_id=$2`,[id,uid])).rowCount>0;}
export async function GET(req:Request,{params}:{params:{id:string}}){const u=currentUser();if(!u)return NextResponse.json({error:'Unauthorized'},{status:401});if(!await owns(params.id,u.id))return NextResponse.json({error:'Not found'},{status:404});const url=new URL(req.url);const format=url.searchParams.get('format');const r=await query<any>(`SELECT submitted_name,email,phone,note,source,consent_at,created_at FROM lead_captures WHERE card_id=$1 ORDER BY created_at DESC LIMIT 5000`,[params.id]);if(format==='csv'){const esc=(v:any)=>`"${String(v??'').replace(/"/g,'""')}"`;const rows=[['Name','Email','Phone','Note','Source','Consent At','Created At'],...r.rows.map(x=>[x.submitted_name,x.email,x.phone,x.note,x.source,x.consent_at,x.created_at])];return new Response(rows.map(row=>row.map(esc).join(',')).join('\n'),{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':`attachment; filename="leads-${params.id}.csv"`}});}return NextResponse.json({leads:r.rows});}
