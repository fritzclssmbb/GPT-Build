import { NextResponse } from 'next/server';import { currentUser } from '../../../lib/auth';import { query } from '../../../lib/db';import { requireSameOrigin } from '../../../lib/security';import { limits,Plan } from '../../../lib/plans';
type CardRow={id:string;slug:string;status:string;display_name:string;title:string|null;company:string|null;bio:string|null;updated_at:string};
export async function GET(){const user=currentUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const result=await query<CardRow>(`SELECT id,slug,status,display_name,title,company,bio,updated_at FROM cards WHERE owner_user_id=$1 ORDER BY updated_at DESC`,[user.id]);return NextResponse.json({cards:result.rows});}
export async function POST(request:Request){const rejected=requireSameOrigin(request);if(rejected)return rejected;const user=currentUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});let plan:Plan='free';if(user.organizationId){const pr=await query<{plan_tier:Plan}>(`SELECT plan_tier FROM organizations WHERE id=$1`,[user.organizationId]);plan=pr.rows[0]?.plan_tier??'free';}else{const pr=await query<{plan_tier:Plan}>(`SELECT plan_tier FROM subscriptions WHERE user_id=$1 AND status IN ('active','trialing') ORDER BY updated_at DESC LIMIT 1`,[user.id]);plan=pr.rows[0]?.plan_tier??'free';}const count=await query<{total:number}>(`SELECT count(*)::int total FROM cards WHERE owner_user_id=$1`,[user.id]);if((count.rows[0]?.total??0)>=limits[plan].cards)return NextResponse.json({error:`${plan} plan card limit reached`},{status:403});const body=await request.json().catch(()=>({}));const displayName=typeof body.displayName==='string'?body.displayName.trim().slice(0,160):'';const slug=typeof body.slug==='string'?body.slug.trim().toLowerCase().replace(/[^a-z0-9-]/g,'').slice(0,100):'';if(!displayName||slug.length<3)return NextResponse.json({error:'Display name and valid slug are required'},{status:400});try{const result=await query<CardRow>(`INSERT INTO cards(owner_user_id,organization_id,slug,display_name,title,company,bio,status) VALUES($1,$2,$3,$4,$5,$6,$7,'draft') RETURNING id,slug,status,display_name,title,company,bio,updated_at`,[user.id,user.organizationId??null,slug,displayName,typeof body.title==='string'?body.title.slice(0,160):null,typeof body.company==='string'?body.company.slice(0,200):null,typeof body.bio==='string'?body.bio.slice(0,3000):null]);return NextResponse.json({card:result.rows[0]},{status:201});}catch(error:any){if(error?.code==='23505')return NextResponse.json({error:'Slug is already in use'},{status:409});throw error;}}
