import { NextResponse } from 'next/server';import { query } from '../../../../../lib/db';import { retryDelaySeconds } from '../../../../../lib/webhooks';
function safeTarget(raw:string){try{const u=new URL(raw);if(process.env.NODE_ENV==='production'&&u.protocol!=='https:')return false;if(!['http:','https:'].includes(u.protocol))return false;const h=u.hostname.toLowerCase();if(h==='localhost'||h==='0.0.0.0'||h==='::1'||h.endsWith('.local')||/^127\\./.test(h)||/^10\\./.test(h)||/^192\\.168\\./.test(h)||/^169\\.254\\./.test(h)||/^172\\.(1[6-9]|2\\d|3[01])\\./.test(h))return false;return true}catch{return false}}
export async function POST(req:Request){if(!process.env.WORKER_SECRET||req.headers.get('authorization')!==`Bearer ${process.env.WORKER_SECRET}`)return NextResponse.json({error:'Unauthorized'},{status:401});const jobs=await query<any>(`WITH picked AS (SELECT d.id FROM webhook_deliveries d JOIN webhook_endpoints e ON e.id=d.endpoint_id WHERE d.status IN('pending','failed') AND COALESCE(d.next_attempt_at,now())<=now() AND e.active=true ORDER BY d.created_at FOR UPDATE OF d SKIP LOCKED LIMIT 25) UPDATE webhook_deliveries d SET status='processing',attempts=attempts+1 FROM picked WHERE d.id=picked.id RETURNING d.id,d.endpoint_id,d.event_name,d.payload,d.attempts`);let delivered=0,failed=0;for(const j of jobs.rows){const endpoint=await query<any>(`SELECT url FROM webhook_endpoints WHERE id=$1 AND active=true`,[j.endpoint_id]);const url=endpoint.rows[0]?.url;try{if(!url||!safeTarget(url))throw new Error('Webhook target is not permitted');const res=await fetch(url,{method:'POST',headers:{'content-type':'application/json','user-agent':'FS-Digital-Identity-Webhook/1.0'},body:JSON.stringify({event:j.event_name,data:j.payload}),redirect:'error'});if(res.ok){await query(`UPDATE webhook_deliveries SET status='delivered',last_http_status=$2,delivered_at=now(),last_error=NULL,next_attempt_at=NULL WHERE id=$1`,[j.id,res.status]);delivered++;}else throw Object.assign(new Error(`HTTP ${res.status}`),{status:res.status});}catch(e:any){const dead=j.attempts>=8;await query(`UPDATE webhook_deliveries SET status=$2,last_http_status=$3,last_error=$4,next_attempt_at=CASE WHEN $2='dead_letter' THEN NULL ELSE now()+($5||' seconds')::interval END WHERE id=$1`,[j.id,dead?'dead_letter':'failed',e?.status??null,String(e?.message??e).slice(0,1000),retryDelaySeconds(j.attempts)]);failed++;}}return NextResponse.json({processed:jobs.rowCount??0,delivered,failed});}
