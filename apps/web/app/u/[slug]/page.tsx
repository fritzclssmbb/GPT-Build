import { notFound } from "next/navigation";import { demoCard } from "@/lib/demo-card";import LeadCapture from "./LeadCapture";
type Props={params:{slug:string}};export default function PublicCardPage({params}:Props){if(params.slug!==demoCard.slug)notFound();return <main><article className="publicCard previewShell"><div className="phone"><div className="avatar">FS</div><div className="eyebrow" style={{marginTop:22}}>FS SOFTWARES · VERIFIED PROFILE</div><h1>{demoCard.name}</h1><p>{demoCard.title}<br/><strong>{demoCard.company}</strong></p><p>{demoCard.bio}</p><div className="actions"><a className="btn btnPrimary" href={demoCard.primaryCtaUrl}>{demoCard.primaryCtaLabel}</a><a className="btn" href={`/api/cards/${demoCard.slug}/vcard`}>Save contact</a><a className="btn" href={`/api/cards/${demoCard.slug}/qr`}>Show QR</a></div><div className="linkList">{demoCard.links.map(l=><a className="linkButton" href={l.url} key={l.label}>{l.label}<span>↗</span></a>)}</div><div className="identityMeta"><span>{demoCard.phone}</span><span>{demoCard.email}</span><span>{demoCard.location}</span></div><LeadCapture slug={params.slug}/></div></article></main>}
