"use client";
import { useState } from "react";
export default function LeadCapture({slug}:{slug:string}){const [sent,setSent]=useState(false);return <form className="leadForm" onSubmit={e=>{e.preventDefault();setSent(true)}}><strong>Share your details</strong><p>Connect with this professional after the presentation.</p><div className="formGrid"><div className="field"><label>Name</label><input required name="name" placeholder="Your name"/></div><div className="field"><label>Email</label><input required type="email" name="email" placeholder="you@company.com"/></div><div className="field full"><label>Message</label><textarea name="note" placeholder="How can we connect?"/></div></div><button className="btn btnPrimary" type="submit">{sent?"Details captured":"Connect"}</button>{sent&&<p className="success">Presentation lead captured. Live deployments post this form to /api/public/cards/{slug}/leads.</p>}</form>}
