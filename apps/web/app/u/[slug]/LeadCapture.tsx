"use client";
import {useState} from "react";
export default function LeadCapture({slug}:{slug:string}){const [state,setState]=useState<"idle"|"sending"|"sent"|"error">("idle"),[message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("sending");setMessage("");const form=new FormData(e.currentTarget);const payload={name:String(form.get("name")||""),email:String(form.get("email")||""),phone:String(form.get("phone")||""),note:String(form.get("note")||""),consent:form.get("consent")==="on"};if(!payload.consent){setState("error");setMessage("Please confirm consent before sharing your details.");return}const r=await fetch(`/api/public/cards/${encodeURIComponent(slug)}/leads`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await r.json().catch(()=>({}));if(r.ok){setState("sent");setMessage("Thank you. Your details were shared successfully.");e.currentTarget.reset()}else{setState("error");setMessage(data.error||"Unable to send your details right now.")}}return <form className="leadForm" onSubmit={submit}><div className="sectionHeading"><div><div className="eyebrow">CONNECT</div><strong>Share your details</strong></div><span className="statusDot">Secure form</span></div><p>Send your contact information directly to this professional.</p><div className="formGrid"><div className="field"><label>Name</label><input required name="name" autoComplete="name" placeholder="Your name"/></div><div className="field"><label>Email</label><input type="email" name="email" autoComplete="email" placeholder="you@company.com"/></div><div className="field full"><label>Phone</label><input name="phone" autoComplete="tel" placeholder="+63 ..."/></div><div className="field full"><label>Message</label><textarea name="note" maxLength={2000} placeholder="How can we connect?"/></div><label className="consentRow"><input required type="checkbox" name="consent"/><span>I agree to share these details with the card owner for professional follow-up.</span></label></div><button className="btn btnPrimary" disabled={state==="sending"||state==="sent"} type="submit">{state==="sending"?"Sending…":state==="sent"?"Details shared":"Connect"}</button>{message&&<p className={state==="error"?"formError":"success"} role="status">{message}</p>}</form>}
