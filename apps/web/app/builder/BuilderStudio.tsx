"use client";
import { useState } from "react";
import { demoCard } from "@/lib/demo-card";
export default function BuilderStudio(){const [card,setCard]=useState(demoCard);const [saved,setSaved]=useState(false);const set=(k:string,v:string)=>{setCard(c=>({...c,[k]:v}));setSaved(false)};return <section className="twoCol"><form className="card" onSubmit={e=>{e.preventDefault();setSaved(true)}}><div className="eyebrow">CARD STUDIO</div><h2>Profile details</h2><div className="formGrid"><div className="field"><label>Full name</label><input value={card.name} onChange={e=>set("name",e.target.value)}/></div><div className="field"><label>Job title</label><input value={card.title} onChange={e=>set("title",e.target.value)}/></div><div className="field full"><label>Company</label><input value={card.company} onChange={e=>set("company",e.target.value)}/></div><div className="field"><label>Phone</label><input value={card.phone} onChange={e=>set("phone",e.target.value)}/></div><div className="field"><label>Email</label><input value={card.email} onChange={e=>set("email",e.target.value)}/></div><div className="field full"><label>Location</label><input value={card.location} onChange={e=>set("location",e.target.value)}/></div><div className="field full"><label>Professional bio</label><textarea value={card.bio} onChange={e=>set("bio",e.target.value)}/></div></div><div className="actions"><button className="btn btnPrimary" type="submit">Save presentation draft</button><a className="btn" href={`/u/${demoCard.slug}`}>Open published card</a></div>{saved&&<p className="success">Draft captured for this presentation session. Production persistence is available through the authenticated Card API.</p>}</form><aside className="previewShell"><div className="phone"><div className="avatar">FS</div><div className="eyebrow" style={{marginTop:18}}>LIVE PREVIEW</div><h2>{card.name}</h2><p>{card.title}<br/><strong>{card.company}</strong></p><div className="chips"><span className="chip">Emerald Network</span><span className="chip">QR ready</span></div><p>{card.bio}</p><a className="btn btnPrimary" href={`mailto:${card.email}`}>Connect now</a><div className="linkList"><span className="linkButton">{card.phone}</span><span className="linkButton">{card.email}</span><span className="linkButton">{card.location}</span></div></div></aside></section>}
