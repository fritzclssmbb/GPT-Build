import Threads from '@/components/Threads';
const rows=[['Profile views','2,846','+18.4%'],['Unique visitors','1,972','+14.1%'],['CTA clicks','1,338','+19.7%']];
const funnel:[string,number][]=[['Views',100],['CTA clicks',47],['Contact saves',17],['Leads',4.5]];
export default function Analytics(){return <main><section className="analyticsHero"><div className="analyticsThreads" aria-hidden="true"><Threads amplitude={.75} enableMouseInteraction /></div><div className="analyticsHeroCopy"><div className="eyebrow">ANALYTICS · ENGAGEMENT INTELLIGENCE</div><h1>See how identity turns into opportunity.</h1><p>Track discovery, interaction, contact saves and lead conversion through a focused executive view.</p><div className="chips"><span className="chip">30-day window</span><span className="chip">Conversion funnel</span><span className="chip">Organization ready</span></div></div></section><section className="grid kpiGrid">{rows.map(([label,value,delta])=><article className="card kpiCard" key={label}><div className="kpiTop"><span className="eyebrow">{label}</span><span className="trend">{delta}</span></div><div className="metric">{value}</div><p>Compared with the previous reporting period.</p></article>)}</section><section className="card funnelCard"><div className="sectionHeading"><div><div className="eyebrow">CONVERSION</div><h2>30-day engagement funnel</h2></div><span className="statusDot">Live model</span></div>{funnel.map(([label,value])=><div className="funnel" key={label}><span>{label}</span><div><i style={{width:`${value}%`}}/></div><strong>{value}%</strong></div>)}</section></main>}
